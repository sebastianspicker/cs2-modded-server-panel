# Copy .env.example to .env and set DEFAULT_PASSWORD (and optionally REDIS_URL) before running.
# Override env vars in .env; do not commit real credentials.
services:
  cs2rcon:
    build:
      context: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      PORT: 3000
    volumes:
      - ./data:/home/container/data
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=64m
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 512m
